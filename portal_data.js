const portalData = {"Roadmap": [{"id": "Roadmap_1Month", "title": "Databricks 1ヶ月ロードマップ", "content": "# Databricks 1ヶ月集中学習ロードマップ\n\n**目標**: 1ヶ月以内で実務レベルのLakehouse構築スキル習得 ＆ Data Engineer Associate 試験合格レベルへ\n**現在のスキル**: Python（通常レベル）、Spark SQL（少し）、Delta/UC/MLflow（未経験）\n**環境**: Databricks Community Edition（無料枠）※一部機能制限あり\n\n> [!WARNING]\n> **Community Editionの制限事項**\n> Community Editionでは、**Unity Catalog、Delta Live Tables (DLT)、Databricks SQLのレイクハウス（SQLウェアハウス）、高度なWorkflows（Jobs）** は利用できません。\n> これらの機能（特に認定試験で最重要のUnity CatalogとDLT）については「公式ドキュメント・座学＋Geminiでの概念理解」を中心に進めるか、可能であれば実務環境やAzure/AWS上での14日間無料トライアル（クラウド側の従量課金は発生）の利用を検討してください。\n\n---\n\n## 🗺️ 1ヶ月の学習スケジュール目安（週10〜15時間想定）\n\n初心者が迷わないよう、優先度順（基礎→データエンジニアリング→ガバナンス/AI）に構成しています。\n\n| 週 | 優先度 | メイントピック | 学習の目的 | 所要時間 | 進捗チェックポイント |\n|:---|:---|:---|:---|:---|:---|\n| **第1週** | 高 | **環境構築 ＆ Delta Lakeの基礎** | レイクハウス・アーキテクチャの理解とDelta形式でのデータ操作 | 約12時間 | Community EditionでDeltaテーブルを作成し、Time TravelやUPDATEを実行できる |\n| **第2週** | 高 | **PySparkとSpark SQLによるETL** | 実務で不可欠なデータ加工・再利用可能なパイプライン処理の習得 | 約15時間 | Python/PySparkを用いて、CSV等の生データをクレンジングし集計できる |\n| **第3週** | 高〜中 | **メダリオンアーキテクチャ & 増分処理** | Bronze→Silver→Goldのパイプライン設計とAuto Loader基礎 | 約12時間 | 複数ノートブックを連携させ、擬似的なメダリオン構造のデータを構築できる |\n| **第4週** | 中 | **Unity Catalog, MLflow, 生成AI機能** | データガバナンスと最新AI機能のキャッチアップ、試験対策 | 約12時間 | UCの3層構造を説明でき、MLflowでモデルを記録するコードが書ける |\n\n---\n\n## 🛠️ 週別アクションプラン＆ハンズオン課題\n\n### 第1週：LakehouseとDelta Lakeの基礎\nまずはDatabricksの根幹である「Delta Lake」の強力な機能を体験します。\n\n*   **具体的な学習トピック**:\n    *   Databricks Community Editionの登録とクラスター起動\n    *   データレイクとデータウェアハウスの違い、レイクハウスの概念\n    *   Delta Lakeの基本動作 (ACIDトランザクション、Time Travel、Z-Order、Optimize)\n*   **推奨公式ドキュメント**:\n    *   [Databricks チュートリアル: 初めてのDelta Lake](https://docs.databricks.com/ja/delta/tutorial.html)\n*   **Geminiに聞くべき質問例**:\n    *   「Delta LakeとParquetの具体的な違いを初心者向けに例え話で教えて」\n    *   「VACUUMコマンドのリテンション期間（デフォルト7日）の理由と、実務での注意点は？」\n*   **ハンズオン課題**:\n    ```python\n    # 1. サンプルデータの読み込みとDelta形式での保存\n    df = spark.read.csv(\"/databricks-datasets/flights/\", header=True, inferSchema=True)\n    df.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"flights_delta\")\n\n    # 2. Time Travelの確認（履歴・バージョンの確認）\n    spark.sql(\"DESCRIBE HISTORY flights_delta\").show(truncate=False)\n\n    # 3. データの更新 (データレイクなのにUPDATEができる体験)\n    spark.sql(\"UPDATE flights_delta SET delay = 0 WHERE delay IS NULL\")\n    \n    # 4. 古いバージョンへの復元（Time Travel）\n    # df_v0 = spark.read.format(\"delta\").option(\"versionAsOf\", 0).table(\"flights_delta\")\n    ```\n\n### 第2週：PySparkとSpark SQLの実践ETL\nPythonの基礎知識を活かして、PySpark DataFrame APIでのデータ変換をマスターします。\n\n*   **具体的な学習トピック**:\n    *   PySpark DataFrame API (Select, Filter, GroupBy, Join, Window関数)\n    *   Spark SQLとPySparkの使い分け・混在テクニック\n    *   UDF (ユーザー定義関数) のパフォーマンスへの影響とPandas UDF\n*   **推奨公式ドキュメント**:\n    *   [PySpark DataFrame チュートリアル](https://spark.apache.org/docs/latest/api/python/getting_started/quickstart_df.html)\n*   **Geminiに聞くべき質問例**:\n    *   「以下のSQLクエリを同等のPySpark DataFrame APIのコードに書き換えて： `SELECT dest, avg(delay) FROM table WHERE origin = 'JFK' GROUP BY dest`」\n    *   「PySparkでSpark SQLのサブクエリと同じことをするにはどうすればいい？」\n*   **ハンズオン課題**:\n    ```python\n    from pyspark.sql.functions import col, avg, desc\n    \n    # 1. 欠損値の処理とデータ型の変換\n    cleaned_df = spark.table(\"flights_delta\").dropna(subset=[\"destination\"])\n    \n    # 2. GroupByと集計・ソート (DataFrame API)\n    summary_df = cleaned_df.groupBy(\"destination\") \\\n        .agg(avg(\"delay\").alias(\"avg_delay\")) \\\n        .filter(col(\"avg_delay\") > 15) \\\n        .orderBy(desc(\"avg_delay\"))\n    \n    # 3. Silver層のテーブルとして保存\n    summary_df.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"flights_silver\")\n    ```\n\n### 第3週：メダリオンアーキテクチャと増分処理\n実務でのETLパイプライン設計のデファクトスタンダードを学びます。\n\n*   **具体的な学習トピック**:\n    *   メダリオンアーキテクチャ (Bronze / Silver / Gold の役割)\n    *   Auto Loader (`cloudFiles`) による効率的な増分データの取り込み（試験用・知識として）\n    *   Delta Live Tables (DLT) の基本構文と概念（試験にも頻出）\n*   **推奨公式ドキュメント**:\n    *   [メダリオンアーキテクチャとは](https://www.databricks.com/jp/glossary/medallion-architecture)\n    *   [Auto Loader とは何か](https://docs.databricks.com/ja/ingestion/auto-loader/index.html)\n*   **Geminiに聞くべき質問例**:\n    *   「Auto Loaderを使うメリットを、通常の `spark.read.stream` と比較して教えて」\n    *   「Data Engineer Associate試験対策として、Delta Live Tables (DLT) でよく出るキーワードや制約事項を5つ教えて」\n*   **ハンズオン課題**（バッチ処理によるメダリオン構造のシミュレーション）:\n    ```python\n    # Bronze処理 (生データの取り込み - 履歴を全て保持)\n    df_raw = spark.read.json(\"/databricks-datasets/structured-streaming/events/\")\n    df_raw.write.format(\"delta\").mode(\"append\").saveAsTable(\"bronze_events\")\n\n    # Silver処理 (フィルタリングとクレンジング - 分析可能な形に)\n    df_silver = spark.table(\"bronze_events\").filter(col(\"action\") == \"Open\").dropDuplicates([\"id\"])\n    df_silver.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"silver_events\")\n\n    # Gold処理 (ビジネスサマリー - BIやダッシュボード向け)\n    df_gold = spark.table(\"silver_events\").groupBy(\"deviceType\").count()\n    df_gold.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"gold_device_summary\")\n    ```\n\n### 第4週：Unity Catalog, MLflow, 生成AI (Mosaic AI) と試験対策\nモダンなデータ基盤に不可欠なガバナンスと、AI連携の基礎を押さえます。\n\n*   **具体的な学習トピック**:\n    *   Unity Catalogによる3層名前空間 (メタストア -> カタログ -> スキーマ -> テーブル)\n    *   アクセスコントロール戦略 (GRANT / REVOKE)\n    *   MLflowによるエクスペリメントトラッキング（Community EditionでもUIタブから確認可）\n    *   Databricks Mosaic AI (AI関数・Genie等の概念)\n*   **推奨公式ドキュメント**:\n    *   [Unity Catalog の概要](https://docs.databricks.com/ja/data-governance/unity-catalog/index.html)\n*   **Geminiに聞くべき質問例**:\n    *   「Unity Catalogの『外部ロケーション (External Location)』と『マネージドテーブル』の違いを、図解するように分かりやすく説明して」\n    *   「Databricks Data Engineer Associate試験の模擬問題を3問作成して」\n*   **ハンズオン課題**（MLflowのトラッキング体験）:\n    ```python\n    import mlflow\n\n    # MLflowを使ったパラメータとメトリクスのトラッキング\n    # 注: Community Editionでは画面右上の \"Experiment\" アイコンから結果を確認できます\n    with mlflow.start_run(run_name=\"My First MLflow Run\"):\n        mlflow.log_param(\"data_version\", \"v1.0\")\n        mlflow.log_metric(\"row_count\", spark.table(\"gold_device_summary\").count())\n        \n        # 実際の運用ではここで機械学習モデルを学習させ、mlflow.spark.log_model() 等で保存します\n        print(\"Logged metadata to MLflow!\")\n    ```\n\n---\n\n## 🤖 [超重要] 強力なAIアシスタントの使い分け戦略\n\n実務スピードを最速にするには、2つのAIの「使い分け」が鍵です。\n\n### 1. Databricks Assistant (ワークスペース内蔵AI)\n**【役割】** コーディングの相棒、ローカルデバッガー、コードジェネレーター\n*   **得意なこと**：ノートブック内のコンテキスト（今読み込んでいるDataFrameのスキーマ名など）を把握した上でのコード生成。\n*   **使い方**：\n    *   エラーが出たら、セルの中で `/fix` を入力して自動修正させる。\n    *   セルにコメントで `# 〇〇を抽出して、Window関数で累積和を出して` と書き、`Cmd + Enter` (または自動保管) でコードを書かせる。\n    *   既存の複雑なコードに `/explain` を適用し、一行ずつ解説させる。\n\n### 2. Gemini Advanced (Gemini Web UI / Antigravity)\n**【役割】** アーキテクト、メンター、試験対策コーチ\n*   **得意なこと**：深い概念の説明、アーキテクチャの比較検討、エラーの背景となる知識の補完、学習プランの軌道修正。\n*   **使い方**：\n    *   **エラーの根本解決**：「Databricks Assistantに `/fix` を頼んでも直らないエラーがあるんだけど、設定レベルで何か間違ってる？ トレースログはこれ：...」\n    *   **アーキテクチャ相談**：「来月から実務で『一晩で50GB増えるログデータ』を処理する。今の知識だとAuto Loader＋DLTが良い気がするけど、この設計の穴はある？」\n    *   **試験対策**：「Data Engineer Associate向けに、Incremental Processing（増分処理）の分野の知識をクイズ形式で出題して。一つ回答するごとにフィードバックをちょうだい。」\n"}], "Week1": [{"id": "Week1_01_Architecture_and_Internals.md", "title": "01_Architecture_and_Internals", "content": "# 第1週①：LakehouseアーキテクチャとDelta Lakeの内部構造\n\n本資料は、Databricksが生まれた背景（なぜData WarehouseやData Lakeではダメなのか）と、「Delta Lakeが裏側でどう動いているのか」というアーキテクチャの骨子を大枠で掴むためのドキュメントです。実務において「いつDatabricksを採用すべきか」「そもそもDelta Lakeとは何なのか」という判断基準（切り分け）ができるようになることを目指します。\n\n## 1. データ基盤の歴史と「Lakehouse」の誕生\n\nデータ基盤には、これまで大きく2つのパラダイムが存在し、それぞれが限界を抱えていました。\n\n### 第1世代：データウェアハウス (DWH) の時代\n*   **特徴:** 構造化データ（表形式データ）を事前に定義したスキーマに取り込み、高速にSQLで集計する。\n*   **強み:** データ品質の堅牢な担保、ACIDトランザクションの完璧なサポート、BIツールとの高い親和性。\n*   **限界:** 機械学習に必要な「非構造化データ（テキスト、画像）」や、スキーマが頻繁に変わるJSONログに対応しきれない。さらにストレージと計算リソース（コンピュート）が密結合しているオンプレ設計の名残があり、データ量が増えると莫大なライセンス・インフラ費用がかかる。\n\n### 第2世代：データレイクの時代（Hadoop/S3など）\n*   **特徴:** クラウド上の安価なオブジェクトストレージ（AWS S3、Azure ADLS）に、あらゆるデータをそのまま（RAWデータとして）放り込む。\n*   **強み:** 安価で論理的に無限の容量。非構造化データも保存可能。\n*   **限界:** 「トランザクション機能」がない。処理途中でジョブが落ちると「半分だけ書き込まれたゴミデータ」が残る。複数人が同時に更新するとデータが壊れる。結果、ただのゴミ捨て場「データスワンプ（データの沼）」と化してしまった。\n\n### 第3世代：Lakehouse (レイクハウス) アーキテクチャ\nDatabricksが提唱し、現在では業界の事実上の標準となったのがレイクハウスです。これは**「データレイクの安価で柔軟なストレージ」の上に「DWHのガバナンスとトランザクション機能」を被せたもの**です。\n\n> [!IMPORTANT]\n> **実務ベースの実践的切り分け：Databricks(Lakehouse)を選ぶべきケース**\n> - テラバイト(TB)・ペタバイト(PB)級のデータを安価に保存しつつ、機械学習（Python）とBI（SQL）の両方で全社的に使い回したい。\n> - ストリーミングデータ（リアルタイム）とバッチ処理を同一のアーキテクチャ・パイプラインで処理したい。\n> - *【逆のケース】数GB〜数十GB程度の比較的小規模なデータで、高度なデータサイエンスや非構造化データの機械学習も不要な場合は、BigQueryやSnowflakeのような純粋なフルマネージドDWHの方が、構築の手間が省けて目的に合致するケースもあります。*\n\n---\n\n## 2. Delta Lake の物理的構造とACIDトランザクションの正体\n\n「ただのデータレイク（S3やADLSのファイル群）なのに、UPDATEやタイムトラベルができる」という魔法は、**Delta Lake**というオープンソースのストレージフォーマット層によって実現されています。\n\nDelta Lakeテーブルの実体をオブジェクトストレージで見ると、以下のようなディレクトリ構成になっています。\n\n```text\n/my_delta_table/\n  ├── _delta_log/          <-- ★これがトランザクションログ（心臓部分）\n  │   ├── 00000..01.json   <-- バージョンごとのコミット履歴\n  │   ├── 00000..02.json\n  │   └── 00000..10.checkpoint.parquet\n  ├── part-00001-xxxx.parquet  <-- ★実際のデータファイル（列指向の高圧縮フォーマット）\n  ├── part-00002-xxxx.parquet\n```\n\n### Delta Lakeの「読み書き」のメカニズム\nDelta Lakeは**書き換え不可能なParquetファイル群**と、**それを管理するJSON（ログ）**の組み合わせで「データの変更」を表現します。\n\n1. **書き込み (WRITE / UPDATE)**\n   - 既存のParquetファイルを直接上書きすることはありません（クラウドオブジェクトストレージの特性上、既存ファイルの任意箇所の上書きは遅くて危険なため）。\n   - 代わりに、**「変更後の値を持った新しいParquetファイル」を新規作成する形で保存**します。\n   - ストーレジへのファイル保存が終わると、最後に `_delta_log` ディレクトリに新しいJSONファイルを作成し、「古いParquetは無効。新しいParquetを使え」というコミットログをアトミック（不可分）に書き込みます。\n\n2. **読み込み (READ)**\n   - クライアントのアナリストはまず `_delta_log` を見に行きます。\n   - ログの記述を辿り「現在有効な最新のParquetファイル群」のリストを一瞬で見つけ出し、不必要な（古い）ファイルは無視して最新データだけを爆速で読み込みます。\n\n### 楽観的並行性制御 (OCC) による競合解決\n実務では「ジョブAとジョブBが、同じテーブルに同時にAPPENDやUPDATEをする」ということが頻発します。Delta Lakeはこれをどう防ぐのでしょうか？\n\nDelta Lakeはロックをかけて他の利用者を待たせることはせず、同時に書き込みを許可します（楽観的）。しかし、コミット（最後の_delta_logへのJSONの書き込み）の瞬間に競合（両者が全く同じバージョン番号「0003.json」を作ろうとする状態）を検知すると、遅れてきた方のジョブは「他の人の変更と自分の書き込み内容が論理的に衝突しないか」を検証します。衝突しなければ自動的に「0004.json」としてやり直し（リトライ）をかけ成功させます。もし同じ行のデータをいじっていた場合は衝突を避けられないため、即座にエラーで落としてクラッシュを防ぎます。\n\n> [!TIP]\n> **実務での運用知見：**\n> 従来のビッグデータETLで「ジョブが途中で落ちたからリカバリしなきゃ」という時、旧来は「どこまでS3に書き込んでいたか」を調べて手動プログラムで半分を消す必要がありました。\n> Delta Lakeなら、コミット失敗＝Parquetは実在してもJSONに書かれないため**一切書き込まれていないのと同じ（トランザクション保護）**になります。エンジニアはエラー原因を直して、ただ**ジョブを再実行**するだけで、データの重複や破損を気にせず安全にリカバリできます。\n"}, {"id": "Week1_02_Schema_and_TimeTravel.md", "title": "02_Schema_and_TimeTravel", "content": "# 第1週②：スキーマ管理とタイムトラベル（実務運用編）\n\nこのドキュメントでは、本番環境のデータ基盤運用で最もデータエンジニアの頭を悩ませる「データの構造変更（スキーマドリフト）」と「ヒューマンエラー・障害によるデータ損失のリスク」に対する、Databricksの強力な解について深掘りします。\n\n## 1. スキーマ・エンフォースメント（強制）と基盤の防御\n\nデータレイク運用が失敗する最大の原因は、「どんな形式のデータでもとりあえず保存できてしまう」という自由度の高さにあります。上流のWebシステムが突然カラム名を変えたり、テストデータ（「hoge」といった無意味なカラム）を誤って混入させたりすると、それを読み込む下流のBIツールや集計バッチが一斉にクラッシュします。\n\nDelta Lakeはデフォルト設定において、「スキーマ・エンフォースメント（強制）」が厳密に働きます。\n*   既存のテーブル定義に存在しない**新しいカラム**を追加しようとすると直ちにエラーになります。\n*   既存のカラムの**データ型が異なる**（例：INTで定義された列に、STRINGの文字データが入ってくる）場合もエラー弾きを行います。\n*   カラム名の大文字・小文字はデフォルトでは区別されません（Case-Insensitive）。\n\n### 実務での判断：「エラーで止める」べきか、「スキーマの自動拡張を許容する」べきか\n「上流で1列新機能データが増えるたびにETLジョブが落ちて、深夜のオンコールでエンジニアが叩き起こされる」のは避けなければいけません。そのため、\"追加\"の列（データの拡張）をシステム的に許容する場合は、スキーマを進化（エボリューション）させます。\n\n```python\n# スキーマの進化（新しいカラムの自動追加）を許可する書き込み\ndf.write \\\n  .format(\"delta\") \\\n  .mode(\"append\") \\\n  .option(\"mergeSchema\", \"true\") \\ # これがスキーマ進化を許容するスイッチ\n  .saveAsTable(\"my_table\")\n```\n\n> [!WARNING]\n> **実務環境の層に応じたベストプラクティス**\n> - **Bronze層（生データ）**: 源泉データをそのまま保持するため、意図しないデータ欠損を防ぐ目的で、どんな列が増えても許容する（`mergeSchema = true` または Auto Loaderにおける該当オプション）設定にするのが実務のデファクトです。\n> - **Silver層 / Gold層**: ここではデータ品質の「厳密さ（SSOT）」が求められます。`mergeSchema`は基本オフにし、集計用のスキーマ変更が必要な場合は明示的に `ALTER TABLE` などのSQLコマンドでエンジニアが構造を手動拡張すべきです。\n\n---\n\n## 2. タイムトラベル（Time Travel）によるデータ復旧と監査\n\n前章（01_Architecture）で「Delta Lakeは既存のParquetファイルを直接上書きせず、変更のたびに新しいParquetファイルを作り、_delta_logに新バージョンとしてJSON記録する」と説明しました。\nこれはつまり、**「古いバージョンのParquetファイルも、クラウドストレージにそのまま残っている」**ということを意味します。これこそが「タイムトラベル機能」の仕組みそのものです。\n\n### 最新の企業実務での活用シナリオ\n\n1. **ヒューマンエラーの取り消し・DR対策 (Disaster Recovery)**\n   うっかり本番環境で `DELETE FROM target_table` と WHERE 句なしで全件削除してしまった場合でも、バックアップから何時間もかけて戻す必要はなく、一瞬で元に戻す（RESTORE）ことができます。\n   ```sql\n   -- 過去の履歴一覧テーブルを確認し、安全だったバージョン番号(version)を特定\n   DESCRIBE HISTORY target_table;\n\n   -- バージョン「10」が安全だった場合、まるごとテーブルの状態をそこへ巻き戻す\n   RESTORE TABLE target_table TO VERSION AS OF 10;\n   ```\n\n2. **機械学習モデルの再現性確保 (Reproducibility)**\n   昨日「95%」の精度が出た機械学習モデルが、今日最新データで学習させたら「60%」に落ちたとします。実務では「アルゴリズムの設定が悪いのか、データそのものの分布が変わったのか」を切り分けるために、設定に問題がないことを証明するため過去時点のピクセルパーフェクトなデータでモデルを再評価する必要があります。\n   ```python\n   # \"2024-03-25\" 時点にテーブルが存在していた状態のデータフレームを正確に抽出してモデルに読ませる\n   df_ml = spark.read.format(\"delta\").option(\"timestampAsOf\", \"2024-03-25 00:00:00\").table(\"features_table\")\n   ```\n\n3. **セキュリティ監査とコンプライアンス**\n   「いつ、どのジョブ（あるいはユーザーのメールアドレス）が、何件のレコードをUPDATEしたか」という操作情報が `DESCRIBE HISTORY` を通じて自動記録されているため、情報システム部門のセキュリティ監査要件をすぐに満たすことができます。\n"}, {"id": "Week1_03_Performance_Optimization.md", "title": "03_Performance_Optimization", "content": "# 第1週③：パフォーマンス最適化とファイル管理\n\nデータエンジニアの実務において、単にシステムからデータを取り込む仕組みを作るだけでなく、「いかに裏側のインフラコストを下げ、高速なユーザークエリ体験を提供するか（パフォーマンスチューニング）」が腕の見せ所です。\nDelta Lakeにおける3つの最重要コマンド (`OPTIMIZE`, `ZORDER`, `VACUUM`) の仕組みと「実務でのスケジューリングの考え方」をまとめます。\n\n## 1. 最重要課題：スモールファイル問題（The Small File Problem）\nストリーミング処理や、1時間ごとの細かなバッチ処理でひたすらデータを `APPEND` していくと、S3やADLS上に「数KB〜数MBの非常に小さなParquetファイル」が数万・数十万と爆発的に溜まっていきます。\nSparkやDatabricksの分散処理エンジンの仕組み上、「1つの巨大な1GBのファイルを1回読み込む」より「1MBの小さなファイルを1000回読み込む」方が、ネットワーク通信やメタデータ展開のオーバーヘッドにより**圧倒的に（体感で数十倍）遅くなります**。\n\nこれを解決するのが `OPTIMIZE` コマンド（ファイル結合・コンパクション）です。\n\n```sql\n-- テーブル内の小さなファイルをかき集め、読み込みに最適なサイズ（約1GB）のファイルに結合・再編成する\nOPTIMIZE target_table;\n```\n\n## 2. Z-Order クラスタリングによる異次元の高速化 (Data Skipping)\nあるビジネス部門から「全期間のデータはいらない。特定の『最新月のデータ』と『特定店舗』しか見ないのに、ダッシュボードの表示が遅すぎる」と苦情がきたとします。\n従来のリレーショナルデータベース(DWH)では、こうした検索用に B-Tree Index（インデックス）を張りますが、データレイクなどのビッグデータ基盤ではインデックスファイル自体の容量が巨大になりすぎて検索が破綻します。\n\n代わりにDelta Lakeで使われるのが **Z-Order** クラスタリングです。\nこれは、指定したカラム（例：日付や店舗ID）の「似た値同士が、物理的に同じParquetファイルに入るように」データを多次元的にソートして保存し直す技術です。\nこれを行うと、検索時に「ああ、探している店舗のデータはファイル名AとCにしか入っていないな」と瞬時に判断し、残りの数千個のファイルを読み飛ばします（Data Skipping）。結果としてクラウドのI/Oコストが劇的に削減されクエリが爆速になります。\n\n> [!IMPORTANT]\n> **実務でのZ-Order判断基準とアンチパターン**\n> - **最適な対象**: アナリストのSQLクエリの `WHERE` 句や `GROUP BY` で非常によく使われるカラム（最大で1〜3個程度が推奨）。例：`tenant_id`, `date`, `device_id`。\n> - **NG（アンチパターン）**: カーディナリティが極端に高い（ユニークな値が多すぎる）列（各行ごとのランダムなUUIDなど）を指定しても、Z-Orderの並べ替えによるグルーピング恩恵を受けにくく、最適化計算時間が無駄にかかるだけになります。\n\n## 3. VACUUM（ゴミ箱を空にする）とコンプライアンス管理\n「タイムトラベルができる（＝古いParquetファイルが残っている）」ということは、最適化のために `OPTIMIZE` や `UPDATE` をくり返せばくり返すほど、使われなくなった古いファイルが無限にストレージに残り続ける（＝クラウドストレージ費用の右肩上がりの増大）ことを意味します。\nこの不要になった古い物理ファイルをクラウド上から「完全に」手放すのが `VACUUM` コマンドです。\n\n```sql\n-- デフォルトでは、7日より古いファイル（かつ最新テーブルの構成に関与しないゴミファイル）のみ削除\nVACUUM target_table;\n```\n\n> [!CAUTION]\n> **実務で直面するVACUUM後の注意点とGDPR要件**\n> `VACUUM` を実行してクラウドから物理削除されたファイルには、もう**絶対にタイムトラベル（復元）することはできません。**\n> GDPR（忘れられる権利）などのコンプライアンス要件で、「ユーザーからアカウント削除依頼が来たら、物理データベースからも30日以内に完全に消去しなければならない」という厳しいルールがある場合、レコードを `DELETE` 実行した後、その該当ファイルがいち早く古くなるのを待って `VACUUM` を高頻度で定期実行するアーキテクチャ設計が必須となります。\n\n---\n\n## ⚙️ 現場でのメンテナンス運用スケジュール（推奨アーキテクチャ）\nベストプラクティスとして、商用環境ではDatabricks Workflows（ジョブ）を使って以下のようなスケジューリングを組みます。\n*   **毎日深夜 (もしくは数時間ごと)**: `OPTIMIZE my_table ZORDER BY (検索キー)` （新しく追加された乱雑なデータを整理）\n*   **週末に1回 (ストレージ最適化)**: `VACUUM my_table RETAIN 168 HOURS`（デフォルトの7日間=168時間を過ぎた不要ファイルを一掃して従量課金コストを落とす）\n"}, {"id": "Week1_04_HandsOn_Master.md", "title": "04_HandsOn_Master", "content": "# 第1週④：週次ハンズオン・マスターコード\n\n今週の資料（アーキテクチャ、スキーマ管理、パフォーマンス最適化）で学んだ概念を、**約30分で一気に全て体感できるPython & SQLのハイブリッドコード**です。文章で読んだものを実際に動かして「体感」へ落とし込んでください。\nDatabricks Community Editionで新規ノートブックを作成し、以下のコードを上から順番にセルへ貼り付けて実行（Shift+Enter）してください。\n\n---\n\n### Step 1: データの投入とDeltaテーブル化（Bronze相当）\n```python\n# 1. 組み込みのサンプルCSVデータセットを読み込み、Dataframeとしてメモリに展開\ndf = spark.read.csv(\"/databricks-datasets/flights/\", header=True, inferSchema=True)\n\n# 2. \"delta\"フォーマットを指定して上書き保存し、データレイクをLakehouseの世界へ持ち上げる\ntable_name = \"practice_flights_delta\"\ndf.write.format(\"delta\").mode(\"overwrite\").saveAsTable(table_name)\n\nprint(\"✅ Data successfully loaded into Delta Lake!\")\n```\n\n### Step 2: DWHライクなACID操作 (UPDATE / DELETE) の体感\n```sql\n%sql\n-- ノートブックで %sql を使うと、そのセルは純粋なSQLセルに切り替わります\n-- 1. まず現在の「遅延(delay)がNULL」の状況（更新前）を確認します\nSELECT delay, count(*) as count FROM practice_flights_delta GROUP BY delay ORDER BY delay ASC LIMIT 5;\n\n-- 2. ★トランザクション処理：遅延がNULLのものをすべて \"0\" に一括更新（S3上のログ更新）\nUPDATE practice_flights_delta SET delay = 0 WHERE delay IS NULL;\n\n-- 3. ★データクレンジングとして、originが特定の空港('ORD')のレコードを削除\nDELETE FROM practice_flights_delta WHERE origin = 'ORD';\n```\n\n### Step 3: タイムトラベルと、誤操作からの劇的ロールバック\n```sql\n%sql\n-- 1. ここまでの基盤操作履歴を確認。先ほどのUPDATEやDELETEがどのようにロギング(_delta_log)されているか見ます。\nDESCRIBE HISTORY practice_flights_delta;\n\n-- 2. 失敗した！やっぱり先程のDELETEを取り消したい！ -> 「RESTORE」コマンドで初期バージョン（version 0）にロールバックします。\n-- （※ 出力された履歴の一番古いversionが0であることを前提としています）\nRESTORE TABLE practice_flights_delta TO VERSION AS OF 0;\n\n-- 確認: 削除されたはずの 'ORD' 空港のレコードが復活しているはずです\nSELECT count(*) FROM practice_flights_delta WHERE origin = 'ORD';\n```\n\n### Step 4: スキーマエボリューション（進化）の実践\n```python\n# 1. 本番環境で「いきなり上流システムから全く知らない『system_info』などのカラムが増えたデータが送られてきた」と仮定します。\ndf_new_schema = spark.sql(\"SELECT *, 'System_A' as source_system FROM practice_flights_delta LIMIT 10\")\n\n# 2. そのまま mode(\"append\") すると通常はエラーになりますが、 option(\"mergeSchema\", \"true\") を付けることで、基盤側が柔軟にテーブル定義を受け入れて拡張します。\ndf_new_schema.write.format(\"delta\").mode(\"append\").option(\"mergeSchema\", \"true\").saveAsTable(\"practice_flights_delta\")\n\n# 確認：最後に「source_system」カラムが空値（NULL）あるいは値ありで追加構成されているのを確認\ndisplay(spark.sql(\"SELECT * FROM practice_flights_delta LIMIT 5\"))\n```\n\n### Step 5: ファイルの一本化と検索の高速化チューニング (OPTIMIZE & ZORDER)\n```sql\n%sql\n-- 実務で頻繁に検索される \"destination\" (目的地) と \"origin\" (出発地) をキーとして指定し、ファイルを多次元ソート・再編成(コンパクション)する\nOPTIMIZE practice_flights_delta ZORDER BY (destination, origin);\n\n-- 最後にVACUUMでゴミ掃除\n-- （※DRY RUNオプションは：実際には消さず、対象となる古ぼけたファイルのリストだけ表示する「安全モード」です）\nVACUUM practice_flights_delta RETAIN 0 HOURS DRY RUN;\n-- ※セーフティガード設定により、0時間の即時VACUUMはエラーになることがあります。実務では通常実行コマンド \"VACUUM practice_flights_delta;\" (デフォルト7日残しで掃除) をスケジューリングします。\n```\n\n> [!TIP]\n> 上記のコードを動かしながら、「ああ、ここを実行した瞬間、裏では新しいParquetファイルと、JSONの_delta_logが書かれたんだな」「OPTIMIZEが走って小さなゴミファイルが一つに固められたんだな」と視覚的に思い描きながら実行できるようになれば、あなたはすでに**優れたデータエンジニアとしての解像度**を獲得しています！\n"}], "Week2": [{"id": "Week2_01_Spark_Architecture_and_LazyEvaluation.md", "title": "01_Spark_Architecture_and_LazyEvaluation", "content": "# 第2週①：Sparkアーキテクチャと「Catalyst」「遅延評価」の裏側\n\n今週はデータエンジニアにとって最も泥臭く、そして最も腕の差が出る「ETL（データ加工）」に特化します。PySparkの構文をただ暗記するのではなく、「裏側でSparkがどう動いているのか」を理解することで、エラー解決やパフォーマンス問題へ実務的に対応できる能力（切り分け力）を養います。\n\n## 1. Apache Sparkの物理アーキテクチャ（大枠の理解）\nDatabricks環境の裏側では、分散処理エンジンであるApache Sparkが動いています。クラスター（コンピュート）は、以下の2つの役割を持つ仮想マシンの集合体で構成されます。\n\n*   **Driver（ドライバーノード）**: 「司令塔」。ユーザーが書いたコード（ノートブックのセル）を読み取り、実行計画を立て、後述するワーカーたちに仕事（Task）を分割して割り振ります。全体の進捗管理を担います。\n*   **Worker/Executor（ワーカーノード）**: 「作業員」。ドライバーから渡された個別のTask（例：この1GBのParquetファイルを読み込んでフィルタリングしろ）を実際に並列実行します。\n\n> [!WARNING]\n> **実務でのトラブルシューティング**\n> 「`OutOfMemoryError (OOM)`」 が出た時、それが「**DriverのOOM**（ `collect()` などを使いすぎて巨大データを司令塔に集めすぎた）」なのか、「**ExecutorのOOM**（ JOIN時にデータが偏りすぎて、一人の作業員のメモリがパンクした）」なのかを切り分けることがデータエンジニアの第一歩です。\n\n## 2. Catalyst Optimizer（最強の最適化エンジン）\n「Pythonで書いたPySpark (DataFrame API)」と「SQLで書いたSpark SQL」、どちらの方が処理が早いでしょうか？\n**答えは「全く同じ速度」です。**\n\nなぜなら、ユーザーがPythonやSQLでどんなに汚いコード（非効率な順番でのフィルタリングなど）を書いても、最終的に **Catalyst Optimizer（カタリスト・オプティマイザ）** というエンジンがコードを解釈し、以下のように「最も無駄のない物理的実行計画」へ自動でコンパイルし直してからJVM（Java Virtual Machine）上で実行するからです。\n\n1. **論理計画:** ユーザーのコードをツリー構造に変換。\n2. **最適化:** 「先にJOINするより、前もってFILTERでデータを削っておく方が通信量が減って速いな」とエンジンが自動で順序を入れ替える（Predicate Pushdown等）。\n3. **物理計画:** クラスターの状況を見て複数の実行案を作り、最もコストの低い方法（例えば Broadcast Hash Join か Sort Merge Join か等）を選択する。\n\n## 3. 「遅延評価 (Lazy Evaluation)」の大原則\nSparkプログラムの関数は、大きく「**Transformation（変換）**」と「**Action（アクション）**」の2つに厳密に別れます。これが「遅延評価」の正体です。\n\n*   **Transformation（変換の予約）**\n    *   関数例：`select()`, `filter()`, `withColumn()`, `groupBy()`, `join()`\n    *   **特徴**: コードを実行（Shift+Enter）しても、**データの計算は一切行われません。** 瞬時にセルが完了し、「こういう手順でデータを加工してね」という設計図（DAG）だけがドライバーノードのメモリに記録されていきます。\n*   **Action（実行の引き金）**\n    *   関数例：`display()`, `show()`, `count()`, `write.saveAsTable()`, `collect()`\n    *   **特徴**: **この関数が呼ばれた瞬間に初めて、これまでに溜め込んだTransformationの設計図がCatalyst Optimizerに渡され、実際のデータ処理（計算）が全ワーカーノードで一斉にスタート**します。\n\n> [!TIP]\n> **実務での判断基準と落とし穴**\n> 「このセル（`df_filtered = df.filter(...)`）は0.1秒で終わるのに、次のセル（`display(df_filtered)`）がやたら5分もかかって遅い」と新人が悩むことがよくあります。\n> これは `filter` が重いのではなく、これまでのすべての変換処理の計算が `display()` というActionによって**初めて実行開始された**ためです。「どこからどこまでが実際に処理されているか」を常に意識してデバッグすることが、ビッグデータ基盤成功の鍵です。\n"}, {"id": "Week2_02_DataFrame_vs_SQL_and_UDFs.md", "title": "02_DataFrame_vs_SQL_and_UDFs", "content": "# 第2週②：DataFrame APIとSQLの実務的な使い分けとUDFの罠\n\nこのドキュメントでは、実際のETLパイプライン開発において「いつPython (DataFrame API) を使い、いつSQLを使うべきか」という設計判断基準と、データエンジニアが絶対に避けるべきパフォーマンスの地雷「Python UDF」について解説します。\n\n## 1. 実務における「SQL」と「DataFrame API」の使い分け戦略\n\n前章の通り、SQLとPySpark (DataFrame) はCatalyst Optimizerによって同じ実行計画に変換されるため、「速度」を理由に使い分ける必要はありません。**「保守性」「可読性」「動的処理」の観点で使い分けます。**\n\n### 🟢 Spark SQL を採用すべきケース\n*   **単純明快なデータ集計や抽出**：アナリストや他部門の人が見てもすぐに理解できるため、BI的な集計ロジック（`GROUP BY`, `CASE WHEN` など）はSQL（`%sql` または `spark.sql(\"SELECT ...\")`）で書く方がチーム全体の保守性が高まります。\n*   **MERGE INTO（アップサート）処理**：Delta Lakeへの複雑なインサート/アップデート処理は、Pythonのメソッドチェーンで書くよりSQLの `MERGE INTO` 構文で書いたほうが圧倒的に直感的で読みやすいです。\n\n### 🔵 PySpark (DataFrame API) を採用すべきケース\n*   **動的なETLパイプライン構築**：例えば「数十個のカラム名の空白をすべてアンダースコア `_` に一括置換したい」場合、SQLでは全カラムを手打ちする必要がありますが、PySparkならPythonの `for` ループを使って動的にメタプログラミングが可能です。\n*   **モジュール化と再利用**：複雑な変換ロジックを関数化し、別ファイル（他のノートブックやPythonスクリプト）に切り出して、複数のジョブから `import` して使い回したい（テストコードも書きたい）場合は、PythonベースのDataFrame APIが必須となります。\n\n> [!IMPORTANT]\n> **実際の現場でのベストプラクティス**\n> Databricks上ではノートブック環境を活かし、**ハイブリッドアプローチ**を取るのが主流です。\n> 生データの複雑なクレンジングやループ処理はPySparkで実装し、最終的にクリーンになったデータフレームを一時ビューとして登録（`df.createOrReplaceTempView(\"temp_silver\")`）してから、集計やMERGE部分は `%sql` マジックコマンドを使ってスッキリと表現する、という棲み分けが非常に強力です。\n\n---\n\n## 2. 最悪のパフォーマンス地雷「Python UDF」とその回避策\n\nPySparkには、標準のSQL関数セット（`pyspark.sql.functions`内にある何百ものビルドイン関数）で表現しきれない「独自の複雑な処理」をユーザーが自作のPython関数で実装できる **UDF（User Defined Function: ユーザー定義関数）** という機能があります。\n\nしかし、**通常のPython UDFは「パフォーマンスの絶対的アンチパターン」です。**\n\n```python\n# 🚫 現場でやってはいけない最悪のアプローチ（通常のPython UDF）\nfrom pyspark.sql.functions import udf\nfrom pyspark.sql.types import IntegerType\n\n# 独自の複雑(？)なPythonロジック\ndef complex_math(salary):\n    return salary * 2 + 100\n\n# UDFの登録\nbad_udf = udf(complex_math, IntegerType())\n\n# 適用（激遅になる）\ndf.withColumn(\"new_salary\", bad_udf(\"salary\"))\n```\n\n### なぜ通常のPython UDFは遅いのか？（アーキテクチャの壁）\nSparkのデータ処理エンジンの中核は**JVM言語（Scala/Java）**で動いています。\n標準のビルドイン関数を使えば全てJVM内で超高速に並列処理されますが、通常のPython UDFを使った瞬間、以下の悲劇（シリアライズ・オーバーヘッド）が全レコードに対して1行ずつ発生します。\n\n1. C++で書かれたSparkのメモリ領域からデータを取り出す。\n2. データを「JVMが理解できる形（Javaオブジェクト）」に変換する。\n3. さらにそれを「Pythonプロセスが理解できる形（Pickle等）」に直列化（シリアライズ）して、ワーカーマシン上のPythonプロセスへ送る。\n4. Python上で1行だけ処理を行う。\n5. 全く逆の順序でJVMに戻す。\n\nこのデータの行ったり来たり（シリアライズ/デシリアライズ）に膨大な時間がかかり、処理は通常の組み込み関数の数万倍遅くなることがあります。\n\n### ✅ 実務での回避策\n1. **第一選択：なんとかして標準の `pyspark.sql.functions` を組み合わせて実現する。**\n   Gemini Advanced等に「このPython処理のロジックを、PySparkの組み込み関数だけで表現して」と頼むのが最も賢い選択です。\n2. **第二選択：Pandas UDF (Vectorized UDF) を使う**\n   どうしても独自のPythonライブラリ（自然言語処理用のspaCyモジュール等）を使う必要がある場合は、Apache Arrow（高速なインメモリカラムフォーマット）を利用して、数十万行を一括配列としてシリアライズなしでPythonプロセスへ渡せる **Pandas UDF** を利用します。これならパフォーマンスの低下を最小限に抑えられます。\n"}, {"id": "Week2_03_Advanced_Transformations.md", "title": "03_Advanced_Transformations", "content": "# 第2週③：実務で必須となる高度なデータ変換技術\n\n実際のETL現場では、「ただ単純にFILTERしてSELECTするだけ」の綺麗なデータパイプラインは稀です。JSONが混じった非構造化ログのパース、数億行同士の結合（Join）の最適化など、データエンジニアが息を吸うように書かなければならない3つの高度な処理について解説します。\n\n## 1. 構造化データの中に潜む「非構造化配列（JSON列）」の展開\n\nWebサーバーのログやAPI連携などにおいて、「1つのカラムの中に、さらに巨大なJSON文字列がそのまま放り込まれている」というケース（セミ構造化データ）が実務では多発します。\nPySparkでは `from_json` と `explode` を組み合わせて、これらのJSONネストをフラットな綺麗なテーブル（Silver層データ）へと展開します。\n\n```python\nimport pyspark.sql.functions as F\nfrom pyspark.sql.types import StructType, StructField, StringType, IntegerType\n\n# 例： 'payload' というカラムに {\"user_name\": \"Ken\", \"age\": 30} という文字列が入っている場合\n# 1. 中身のスキーマを定義する\njson_schema = StructType([\n    StructField(\"user_name\", StringType(), True),\n    StructField(\"age\", IntegerType(), True)\n])\n\n# 2. 文字列を構造化データ(Struct)にパースし、親のDataframeのカラムとして展開する\ndf_parsed = df.withColumn(\"parsed_payload\", F.from_json(F.col(\"payload\"), json_schema)) \\\n              .select(\n                  \"id\",\n                  \"timestamp\",\n                  F.col(\"parsed_payload.user_name\").alias(\"name\"), # 中身を直接抽出\n                  F.col(\"parsed_payload.age\")\n              )\n```\n\n## 2. 結合（Join）戦略とデータの偏り（Data Skew）の回避\n\nビッグデータにおいて、10億行のテーブル(Fact)と、別の100万行のテーブル(Dimension)を普通にJoinすると、ネットワークを介した膨大なデータのシャッフル（再配置交換）が発生し、ジョブが数時間終わらないかOOM（メモリ不足）でクラッシュします。\n\n### 実務での判断：Broadcast Hash Join (BHJ) への強制\n片方のテーブルが比較的小さい（設定の目安は10MB以内だが、実務では数百MBまで引き上げることが多い）場合、わざわざネットワーク越しに対向データをシャッフルして交換するのではなく、**「小さなテーブルを、全てのワーカーマシンのメモリ（Executor）に事前にコピー（Broadcast）してバラ撒いておき、巨大テーブルは一切動かさずに各ノード内でローカル結合させる」** というのがビッグデータ処理の定石です。\n\n```python\n# Databricks(Spark)に「department_dfは小さいから全ノードにBroadcastしろ」とヒントを与える\ndf_joined = huge_sales_df.join(\n    F.broadcast(department_df), # ← これだけで数時間のジョブが数分で終わることがある\n    \"department_id\",\n    \"left\"\n)\n```\n\n> [!WARNING]\n> **Data Skew（データの偏り）への警戒**\n> 「特定の商品（例：年末の福袋）だけが極端に売れすぎて、ログの90%が `item_id = 9999` に偏っている」といった場合、通常の手法でJoinをかけると、特定の1台のワーカーノードだけに処理が集中し、そこだけがパンクしてクラッシュします（Data Skew）。\n> DatabricksのPhotonエンジンや最新のDatabricks Runtime上では、こうしたSkewを自動検知して分離する機能（AQE: Adaptive Query Execution）がデフォルトで働きますが、根本的なアーキテクチャ設計として「偏りがひどい列はJoinのキー単体にしない」などの考慮が必要です。\n\n## 3. Window関数による強力な集計と重複排除\n\nRDBの世界でもおなじみですが、データ内の特定のキー（例：顧客ID）ごとに区画（Window）を作り、その「区画内での順位」や「移動平均」「直前の行との差分」などを計算する機能が Window関数 です。\n実務で最も使われるのが**「最新の更新レコードだけを残して重複排除（Deduplication）する」**処理です。\n\n```python\nfrom pyspark.sql.window import Window\n\n# 顧客IDごとに区画を作り、更新日時(updated_at)の新しい順(desc)に並べるルールを定義\nwindow_spec = Window.partitionBy(\"customer_id\").orderBy(F.desc(\"updated_at\"))\n\n# 各区画（各顧客）の中で1位（つまり一番新しい更新レコード）のみを残す\ndf_latest = raw_df.withColumn(\"row_num\", F.row_number().over(window_spec)) \\\n                  .filter(F.col(\"row_num\") == 1) \\\n                  .drop(\"row_num\")\n```\nこのWindow関数による処理は、単純な `dropDuplicates` や `groupBy` では対応できない「どの行を優先して残すか」というビジネス要件に応えるための、Silver層構築における最重要テクニックです。\n"}, {"id": "Week2_04_HandsOn_Master.md", "title": "04_HandsOn_Master", "content": "# 第2週④：週次ハンズオン・マスターコード (PySpark実践ETL編)\n\n今週学んだ「Sparkの最適化機構を信じること」「UDFを避け組み込み関数を活用すること」「Window関数やJSONパース」といった実学を、約30分で一気に体験できる強力なPySparkマスターコードです。\n\nDatabricks Community Editionで新規ノートブックを作成し、言語を「Python」に設定の上、以下のコードを順に実行（Shift+Enter）して、洗練されたデータエンジニアリングの世界を体感してください。\n\n---\n\n### Step 1: 疑似的な汚い生データ (RAW JSON) の作成\n実環境でよくある、「JSONフォーマットでログが飛んでくる」状況を再現します。\n```python\nfrom pyspark.sql.types import StructType, StructField, StringType, IntegerType\nimport pyspark.sql.functions as F\n\n# 意図的にネスト（入れ子）になり、かつ重複を含む汚いテストデータの作成\ndata = [\n    (1, '{\"user\": \"Alice\", \"score\": 85, \"device\": \"iOS\"}', \"2024-03-01 10:00\"),\n    (2, '{\"user\": \"Bob\", \"score\": null, \"device\": \"Android\"}', \"2024-03-01 10:05\"),\n    (1, '{\"user\": \"Alice\", \"score\": 90, \"device\": \"iOS_Update\"}', \"2024-03-02 11:00\"), # Aliceの最新の更新\n    (3, '{\"user\": \"Charlie\", \"score\": 75, \"device\": \"Web\"}', \"2024-03-02 12:00\")\n]\n\ncolumns = [\"log_id\", \"json_payload\", \"updated_time\"]\nraw_df = spark.createDataFrame(data, columns)\n\nprint(\"--- 元の汚いデータ（Bronze相当） ---\")\ndisplay(raw_df)\n```\n\n### Step 2: 組み込み関数を駆使したJSONのパースと展開\nPythonの標準ライブラリ(`json`)やUDF（自作関数）は決して使わず、すべてPySparkのネイティブ（JVM上で爆速に動く）関数だけで処理します。\n```python\n# 1. 文字列の中にあるJSONの構造（スキーマ）を定義\npayload_schema = StructType([\n    StructField(\"user\", StringType(), True),\n    StructField(\"score\", IntegerType(), True),\n    StructField(\"device\", StringType(), True)\n])\n\n# 2. `from_json`関数で構造化し、`select`の中で `.*` を使って全カラムを一気にフラットに展開\nparsed_df = raw_df.withColumn(\"parsed\", F.from_json(F.col(\"json_payload\"), payload_schema)) \\\n                  .select(\n                      \"log_id\", \n                      \"updated_time\",\n                      \"parsed.*\" # これで user, score, device カラムが一気に生成されます\n                  ) \\\n                  .withColumn(\"updated_time\", F.to_timestamp(\"updated_time\", \"yyyy-MM-dd HH:mm\"))\n\ndisplay(parsed_df)\n```\n\n### Step 3: Window関数を用いた重複排除（最新データのみ残す）\nユーザーID (log_id) が被っているレコード（Aliceのもの）のうち、更新日時(updated_time)が最も新しいものを「真のレコード」として採用します。\n```python\nfrom pyspark.sql.window import Window\n\n# log_idごとに区画を作り、日時が新しい順に並べる\nwindow_spec = Window.partitionBy(\"log_id\").orderBy(F.desc(\"updated_time\"))\n\n# 各区画で「1位（＝最新）」の連番を振り、1のものだけをフィルタリング\nlatest_df = parsed_df.withColumn(\"row_rank\", F.row_number().over(window_spec)) \\\n                     .filter(F.col(\"row_rank\") == 1) \\\n                     .drop(\"row_rank\")\n\nprint(\"--- Aliceの古いログが消え、最新のレコードだけになることを確認 ---\")\ndisplay(latest_df)\n```\n\n### Step 4: JOIN戦略と、遅延評価(Lazy Evaluation)の確認\nマスタテーブル（ごく小さい）を作成し、先ほどのデータと結合(Join)します。\n```python\n# ごく小さいマスタデータの作成\nmaster_data = [(\"iOS_Update\", \"Mobile\"), (\"Android\", \"Mobile\"), (\"Web\", \"Desktop\")]\nmaster_df = spark.createDataFrame(master_data, [\"device_name\", \"category\"])\n\n# ※ ここまで全くデータそのものの計算・結合は走っていません（遅延評価）！設計図が作られているだけです。\n\n# Broadcastのヒントをエンジンに与え、全てのノードのメモリにマスタデータをバラ撒いて最強の最適化を引き出す\nfinal_silver_df = latest_df.join(\n    F.broadcast(master_df), \n    latest_df[\"device\"] == master_df[\"device_name\"], \n    \"left\"\n).drop(\"device_name\") # 不要な重複キーを最後に削除\n\n# 欠損値(scoreがnullなBob)にデフォルト値0を埋める\nfinal_silver_df = final_silver_df.fillna({\"score\": 0, \"category\": \"Unknown\"})\n\nprint(\"--- 完成した綺麗なデータ（Silver相当） ---\")\n# 💡 display() (アクション) が呼ばれたこの瞬間に、これまでの Step 2〜4 の全ての変換設計図が\n# Catalyst Optimizerによって最善の手順に並べ替えられ、一気に実行計算されます。\ndisplay(final_silver_df)\n```\n\n> [!TIP]\n> 泥臭いクレンジング処理をこれだけスマートかつ宣言的に（HowではなくWhatだけを関数につなげて）記述できるのが、PySpark DataFrameの最大の魅力です。\n> このノートブックコードの処理の流れ（JSON展開 -> 重複排除 -> マスタBroadcast Join -> NULL埋め）は、そのまま実務の現場のSilver層パイプラインとして即戦力で使える設計になっています！\n"}], "Week3": [{"id": "Week3_01_Medallion_Architecture_Theory.md", "title": "01_Medallion_Architecture_Theory", "content": "# 第3週①：実務現場の「メダリオンアーキテクチャ」設計思想\n\n先週はPySparkを使った個別のロジック（フィルタリング、連結、重複排除など）を学びました。今週は、それらの処理を**「どういう順序で、どういう段階を踏んでテーブルにするか」**というアーキテクチャ（大枠の設計図）について学びます。\n\nDatabricksが提唱し、現在ではAWSやAzure上の一般的なETLでもデファクトスタンダードとして採用されている「メダリオンアーキテクチャ」の理論と実務展開を徹底解剖します。\n\n## 1. なぜアーキテクチャ層を分けるのか？（過去の失敗）\nかつてのデータ基盤では、S3(データレイク)にあるCSVを直接読み込んで複雑なJoinや集計を数百行のSQLで一気に書き、いきなりBI用テーブルを作っていました。\n*   **問題点:** バグがあった時、「元データがおかしいのか」「結合がおかしいのか」「集計がおかしいのか」が全く特定できず、リカバリのために生データを最初から全て再スキャン（全件洗い替え）する必要があり、莫大なコストと時間がかかりました。\n\nこの属人化とメンテナンス地獄を解消するためにデータを**品質レベル別に物理的・論理的に分離**したのがメダリオンアーキテクチャです。\n\n## 2. 各層の実務的役割とアーキテクト設計の基準\n\n### 🥉 Bronze（ブロンズ）層：生データ（RAW）の履歴保管庫\n*   **役割:** 外部のS3、API、Kafkaなどから届いたJSONやCSVなどを、**一切の加工（フィルタリングや型変換すら）を行わずに** まずDelta Lake形式で保存する層。\n*   **データ構造:** `APPEND`（追記）のみ。過去の全ての履歴が蓄積される。変更や削除は基本行わない。\n*   **実務での設計基準:** \n    *   システム障害時や、「1年前の生データってなんだったっけ？」と監査が入った時の絶対的な「リカバリの起点」になります。\n    *   スキーマの強制（Schema Enforcement）はオフにし、上流からのどんな汚いカラムでも（エラーで落とさずに） 일단受け入れる `mergeSchema` 運用が基本です。\n    *   長期間保有するためストレージコストが膨らみやすいので、必要に応じて「1年以上前のデータは安いストレージクラスへ移動する（Tiers化）」などの検討が必要です。\n\n### 🥈 Silver（シルバー）層：企業共有の信頼できる単一の情報源 (SSOT)\n*   **役割:** ブロンズのデータを「分析できるように」綺麗にした層。\n*   **処理内容:** NULL値の補完や除去、JSONの展開（`from_json`）、型変換（String->Date）、別テーブルとの結合によるマスタ付与、Window関数を用いた重複排除などをゴリゴリに行います。\n*   **データ構造:** `APPEND` だけでなく、`UPDATE` や `MERGE INTO` (アップサート) を多用します。\n*   **実務での設計基準:**\n    *   「データサイエンティストが機械学習をするならSilverを直接見れば良い」という状態にします。\n    *   全社で使い回すため、**パフォーマンスチューニング（OPTIMIZEやZ-ORDER）はこの層に最も注力**します。\n    *   カラム名の命名規則（スネークケースなど）を全社基準で統一・強制します。\n\n### 🥇 Gold（ゴールド）層：ビジネス用途・特定部署向けの集計データ\n*   **役割:** BIツールやダッシュボードが数秒で表示できるよう、非常に軽く集計された結果（レポーティング）層。\n*   **処理内容:** 「日別・店舗別の売上合計」「月次アクティブユーザー」など、業務に直結した高度な `GROUP BY` や集計関数（Sum, Count）を実行した結果を置きます。\n*   **実務での設計基準:**\n    *   Gold層のテーブルは「いつでも再作成できる使い捨て」の感覚でOKです。Silverまでのパイプラインが正しければ、Silverから数分で再計算(クリーンナップ)できるからです。\n    *   ダッシュボードから直接検索が飛んでくるため、読み取りに特化したテーブル設計（必要であれば特定キーでの再ソートなど）を行います。\n\n> [!TIP]\n> **実務でのアーキテクチャ省略に関する判断**\n> 「Silver層をわざわざの実テーブル(Parquet)として保存するとストレージコストが勿体ないんじゃないか？」という疑問を持つアーキテクトもいます。\n> たしかに**Silverは実テーブルではなく「SQLのVIEW」**として定義し、読み込み時にオンザフライで変換させることも可能です。しかし、**「何千人もが毎日VIEWを叩くとその都度コンピュート（計算リソース）課金が発生し、逆にクラウドコストが爆発する」**ため、一度だけ前計算として「実テーブル (Delta format)」に落としてしまったほうが、長期的には劇的に安上がりで高速になるというのがベストプラクティスです。\n"}, {"id": "Week3_02_AutoLoader_and_Streaming.md", "title": "02_AutoLoader_and_Streaming", "content": "# 第3週②：Auto Loaderと無限にスケールする増分処理（ストリーミング）\n\n前章（メダリオン）でアーキテクチャの箱を決めたら、次はその箱と箱の「データの受け渡し（インジェスチョン）」パイプラインを作ります。\nここでは「毎日・毎秒、クラウドストレージにファイルが無限に届き続ける状況」をどうやって効率的に処理するか（特にBronze層への取り込み）に焦点を当てます。\n\n## 1. 旧来のバッチ処理の限界\nクラウドストレージの特定ディレクトリ（例: `s3://raw-landing-bucket/logs/`）に、毎日JSONファイルが追加されるとします。\n従来の方法（`spark.read.json(\"s3://.../*\")`）で実行すると、バッチジョブが起動するたびに「ディレクトリ内のすべてのファイルリストを取得（LIST操作）」し、「今回のジョブで対象となったファイルと過去のファイルを比較」しなければなりません。\n**ファイルが数百万個に達すると、この最初の「ファイルリスト取得（ディスカバリ）」だけで1時間以上かかり、処理が完全に破綻します。**\n\n## 2. Databricks Auto Loader (`cloudFiles`) の仕組み\nこの課題を根本から解決するのが、Databricksが最も推している「**Auto Loader (オートローダー)**」機能です。\n\nAuto Loaderは、S3などのクラウドストレージに「新しく到着したファイルだけ」を高速で検知し、未処理のインクリメンタル（増分）データのみを取り込むストリーミング機構です。\n内部では以下の2つのモードで動きます。\n\n*   **Directory Listing モード (デフォルト)**: 定期的にディレクトリを最速でスキャンし、過去に処理した履歴（Checkpointsディレクトリ内のRocksDB）と照合します。数十万ファイル程度までならこれで高速に動きます。\n*   **File Notification モード (エンタープライズ向け)**: S3なら「AWS SQS（キュー）」や「EventBridge」を背後で自動設定します！ ファイルが置かれた瞬間にクラウド側から「置かれたよ」という通知だけを受け取り直接読みに行くため、**フォルダ内に数億個のファイルがあろうと、数秒で新ファイルだけを特定**して読み込みます。\n\n```python\n# 実務でのAuto Loader(cloudFiles)の基本コード\n\n# 1. 読み込み (ストリーミングとして定義する)\nraw_df = spark.readStream.format(\"cloudFiles\") \\\n    .option(\"cloudFiles.format\", \"json\") \\\n    .option(\"cloudFiles.schemaLocation\", \"/tmp/checkpoints/schema/\") \\ # 新しい列が増えたら自動推論\n    .load(\"/mnt/raw_landing_zone/\")\n\n# 2. 書き込み (Bronze層へ)\nraw_df.writeStream.format(\"delta\") \\\n    .option(\"checkpointLocation\", \"/tmp/checkpoints/bronze/\") \\ # 「どこまで読み終わったか」を完璧に記憶する\n    .trigger(availableNow=True) \\ # ★実務で最強のオプション (後述)\n    .toTable(\"catalog.schema.bronze_logs\")\n```\n\n## 3. 実務のベストプラクティス： `Trigger Available Now`\n\n「ストリーミング」と聞くと、「クラスター（サーバー）を24時間稼働させっぱなしにして、リアルタイムで待ち受けなければならないのか？ そんなの費用が持たない」と思われがちです。\n\nここで登場するのが、構造化ストリーミングの最強のオプション **`Trigger(availableNow=True)`** です。\n\nこれは「**バッチクラスターでストリーミング機能を使い倒す**」というアプローチです。\n1. 深夜1時にバッチジョブ（クラスター起動）。\n2. このコードが走ると、Auto Loaderは過去のCheckpointを読み、「前回の実行から現在までに溜まった新ファイル（差分）」を**全て一度だけ**読み込み・処理します。\n3. 処理が終わったら、ジョブ終了とともに自らストリーミングを停止し（クラスターもシャットダウン）、次の実行まで眠りにつきます。\n\n> [!IMPORTANT]\n> **実務での判断基準**\n> 単純な日次バッチ処理（Daily Batch）であっても、通常の `spark.read` を使って「昨日の日付フォルダだけを読む」ように開発するのではなく、**最初からAuto Loader + `Trigger availableNow=True` のストリーミング構文で設計する**のがモダンデータエンジニアリングの定石です。\n> これにより、深夜バッチが失敗して数日止まっていたとしても、次回起動時に「未処理分を全て勝手に再開・消化」してくれます。エンジニアが手動で「13日分と、14日分をパラメーターで流し直して…」操作をする必要が完全になくなり、運用工数が激減します。\n"}, {"id": "Week3_03_DeltaLiveTables_DLT.md", "title": "03_DeltaLiveTables_DLT", "content": "# 第3週③：Delta Live Tables (DLT) とデクララティブ基盤の真髄\n\nノートブックにストリーミング（Auto Loader）を使ったETLコードを書けるようになっても、複数の巨大なデータパイプラインを運用していると必ず限界が来ます。\n「あるノートブックは毎時動かして、別のノートブックは毎日動かす」「途中でエラーが出たら、次のジョブ（ノートブックタスク）にどう伝えるか？」「テスト環境と本番環境でストレージパスをどう書き直すか？」等、インフラ運用・依存関係の管理の**コード外の泥臭い対応**に時間が割かれます。\n\nこれらを解決し、データエンジニアリングの自動化を極限まで押し上げたのが「**Delta Live Tables (DLT)**」です。\n\n## 1. 手順(How)から宣言(What)へのパラダイムシフト\n旧来のETLは「**How（どうやって作るか）**」を記述していました。\n*   *「AフォルダからJSONを読み込んで、XとYに変換して、一時保存して、それを別ノートブックで読み込み、Bテーブルに書き込むための空箱を作り... もし失敗したらリトライを3回する」*\n\nDLTは「**What（私はどんなデータのテーブルが欲しいか）**」だけを記述する（デクララティブ／宣言型パイプライン）仕組みです。\n*   *「私はAというテーブルから不要な空白を取り除き、クリーンになった『Bテーブル』が欲しい。おしまい」*\n\nこれだけをコード（SQLやPython）で宣言しておけば、Databricksエンジンが背後で**DAG（有向非巡回グラフ：データの依存関係と流れる順序）を自動で作成し、インフラのプロビジョニング、Auto Loaderによるストリーミング処理、エラー復旧をすべて勝手に行ってくれます。**\n\n## 2. Expectations による強固なデータ品質管理 (DQC)\n\n認定試験 `Data Engineer Associate` において、**必ず出題され合否を分ける**のがDLTの制約（Expectations - エクスペクテーション）機能です。\n\nSilver層で「IDがNULLのレコードは絶対に排除したい」「金額がマイナスだったら何かがおかしいのでジョブ全体を止めたい」というデータ品質ルールを、DLTではテーブル定義に一行付け加えるだけで自動監視（かつダッシュボード化）してくれます。\n\n制約違反時の対応（パニック時のアクション）には**3つのレベル**があり、実務でもこれを使い分けます。\n\n### 🟡 レベル1：監視するだけ (Dropしない・止めない)\n```sql\nCREATE STREAMING LIVE TABLE silver_users (\n  -- CONSTRAINT [制約名] EXPECT ([条件式])\n  CONSTRAINT valid_age EXPECT (age > 0)\n) AS SELECT * FROM stream(LIVE.bronze_users);\n```\n*   **挙動:** `age` がマイナスの異常行があっても、そのままSilverテーブル（下流）へ流します（保存します）。ただし、DLTの品質監視画面には「何行の違反があったか」のメトリクス（警告）がカウントされ記録されます。最初はこれで本番データの汚さを調査します。\n\n### 🟠 レベル2：不正な行をポイ捨てして続行 (Drop Row)\n```sql\nCREATE STREAMING LIVE TABLE silver_sales (\n  CONSTRAINT valid_id EXPECT (id IS NOT NULL) ON VIOLATION DROP ROW\n) AS SELECT * FROM stream(LIVE.bronze_sales);\n```\n*   **挙動:** `id` がNULLの行は「ゴミ」とみなして捨て去り、**正しい行に関するデータ処理だけを続行して**Silverテーブルに保存します（クリーンな状態を保つ）。\n\n### 🔴 レベル3：異常検知で即座にパイプラインを止める (Fail Update)\n```sql\nCREATE STREAMING LIVE TABLE gold_financial_report (\n  CONSTRAINT valid_amount EXPECT (total_amount >= 0) ON VIOLATION FAIL UPDATE\n) AS SELECT * FROM LIVE.silver_sales;\n```\n*   **挙動:** 会計・財務システムなど、「たった1行でも不正データがあったら、それを用いて作られたダッシュボードを社長に見せるわけにはいかない」という絶対の品質が求められる場合に設定します。1行でもマイナス金額が入ってきた瞬間、パイプラインの更新全体の実行を**強制ブロックして失敗（エラー停止）**させ、エンジニアにアラートを飛ばさせます。\n\n> [!TIP]\n> **実務でのアーキテクチャの棲み分け**\n> - 「手動のノートブックETL（Week 2）」＝ 柔軟で複雑なアドホック処理や小規模バッチ向け。\n> - 「DLT（Delta Live Tables）」＝ プロダクション（商用本番）として長期稼働させる、数十〜数百のテーブルが絡み合う全社の大規模データパイプライン向け。運用者が退職しても仕組みが自動で回る堅牢さがあります。\n"}, {"id": "Week3_04_HandsOn_Master.md", "title": "04_HandsOn_Master", "content": "# 第3週④：週次ハンズオン・マスターコード (ストリーミング＆メダリオン編)\n\n今週学んだメダリオンアーキテクチャ（Bronze -> Silver -> Gold）の全体像と、「Auto Loaderによるストリーミング処理（Trigger availableNowの強さ）」を、約30分で一気に構築・体感できるPySparkのマスターコードです。\n\nノートブックをひとつ用意し、以下の3ステップを順に実行してください。（※ 実際のDLTの構文はCommunity Editionで実行制限があるため、ここでは同等の効果を持つPySparkの**構造化ストリーミング（Structured Streaming）**を用いて再現します。）\n\n---\n\n### 事前準備：ダミーのファイル（生データ）を置く疑似S3の作成\n```python\n# 1. 疑似的なクラウドストレージの着信フォルダ（Landing Zone）をDBFS上に作成\nlanding_zone = \"/tmp/learning_week3/landing/\"\ndbutils.fs.rm(landing_zone, True) # 初期化\ndbutils.fs.mkdirs(landing_zone)\n\n# 2. ダミーのJSONファイルを到着させます\ndbutils.fs.put(landing_zone + \"file1.json\", '{\"id\": 1, \"action\": \"click\", \"user_id\": 100}', True)\ndbutils.fs.put(landing_zone + \"file2.json\", '{\"id\": 2, \"action\": \"login\", \"user_id\": 101}', True)\n\nprint(\"✅ Landing Zone (S3相当) に新しいJSONファイルが2つ到着しました！\")\n```\n\n---\n\n### Step 1: 【Bronze層】Auto Loaderによる生データのストリーミング取り込み\n```python\n# Auto Loader (cloudFiles) を使って、指定フォルダに「新しく到着したファイル」だけを追跡して取得\nbronze_df = spark.readStream.format(\"cloudFiles\") \\\n    .option(\"cloudFiles.format\", \"json\") \\\n    .option(\"cloudFiles.schemaLocation\", \"/tmp/learning_week3/schema/bronze/\") \\\n    .load(landing_zone)\n\n# 取得した生データを、加工せずに「Bronzeテーブル」へ書き込む\n# ★重要: trigger(availableNow=True) により、「今溜まっている未処理分」だけを処理してピタッと終了する（バッチ処理として機能）\ncheckpoint_path_bronze = \"/tmp/learning_week3/checkpoints/bronze/\"\n\nquery_bronze = bronze_df.writeStream.format(\"delta\") \\\n    .option(\"checkpointLocation\", checkpoint_path_bronze) \\\n    .trigger(availableNow=True) \\\n    .toTable(\"bronze_user_events\")\n\nquery_bronze.awaitTermination() # 処理の完走を待つ\n\nprint(\"🥉 Bronze Table created! 生データが履歴として保存されました。\")\ndisplay(spark.table(\"bronze_user_events\"))\n```\n\n---\n\n### Step 2: 【Silver層】クリーンな共有データ (SSOT) への変換\nBronzeに変更（新しいレコード）があった場合のみ、それを読み込んでフィルタリング処理（クレンジング）を行います。今回は「IDが1のデータは不正なので削除する」という疑似ルール(DLTのDrop Row相当)を実装します。\n\n```python\nimport pyspark.sql.functions as F\n\n# Bronzeテーブルの更新分をストリームとして読み込む (これも増分処理)\nstream_bronze = spark.readStream.format(\"delta\").table(\"bronze_user_events\")\n\n# クレンジング処理 (ID=1を除外 = 不正パケットのフィルタリング)\nsilver_df = stream_bronze.filter(F.col(\"id\") != 1)\n\n# Silverテーブルとして出力\ncheckpoint_path_silver = \"/tmp/learning_week3/checkpoints/silver/\"\n\nquery_silver = silver_df.writeStream.format(\"delta\") \\\n    .option(\"checkpointLocation\", checkpoint_path_silver) \\\n    .trigger(availableNow=True) \\\n    .toTable(\"silver_user_events\")\n\nquery_silver.awaitTermination()\n\nprint(\"🥈 Silver Table created! (クレンジング完了・分析可能な美しいデータ)\")\ndisplay(spark.table(\"silver_user_events\"))\n```\n\n---\n\n### Step 3: 【Gold層】ビジネス用の高度な集計結果の作成\nGold層への更新は、ストリーミングで行うかバッチ(全件書き換え)で行うかは要件次第ですが、基本的にはダッシュボードが見に来るための完全切り離し・読み取り専用のバッチ集計でOKなケースが多いです。\n\n```python\n# Silver層からデータをバッチで読み込み、ビジネス要件（Actionごとの回数集計）を行う\ngold_df = spark.table(\"silver_user_events\") \\\n               .groupBy(\"action\").count()\n\n# 重い集計結果を計算し終えた状態で、Goldテーブルへ上書き（上流からの再作成）保存\ngold_df.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"gold_action_summary\")\n\nprint(\"🥇 Gold Table created! (ダッシュボード表示用の爆速集計テーブル)\")\ndisplay(spark.table(\"gold_action_summary\"))\n```\n\n> [!TIP]\n> **追加ハンズオン（Auto Loaderの破壊力を知る）**\n> 1. 上記のStep3まで完了したら、もう一度一番上の「事前準備のセル」に行き、新しいファイル (`file3.json`) だけをLanding Zoneに投下（追加でput）してみてください。\n> 2. その後、Step 1〜3 をもう一度実行させます。\n> 3. ブロンズ・シルバーのストリーミングは「file1, file2」はすでに処理済み(Checkpointに記載済み)であることを知っているため、完全に無視し、**数百万ファイルの海の中から「新しく置かれたfile3」だけの数バイト分を極少リソースでスキャンして追記します**。これを体感できたとき、ストリーミング増分処理の本当の価値が理解できるはずです。\n"}], "Week4": [{"id": "Week4_01_UnityCatalog_Governance.md", "title": "01_UnityCatalog_Governance", "content": "# 第4週①：Unity Catalog (UC) による最強のデータガバナンス\n\nこれまでの3週間で、私たちは「高速で」「壊れにくく」「美しい」メダリオンアーキテクチャのデータ基盤を作ってきました。しかし、いざこれを全社（1000人の社員）へ展開しようとした瞬間、最大の壁にぶち当たります。\n**「誰に、どのデータを見せていいのか（見せてはいけないのか）」**というガバナンスとセキュリティの問題です。\n\n## 1. レガシーなHiveメタストアの限界（なぜUCが必要か？）\n従来のDatabricks（ワークスペースごとのHiveメタストア）では、「テーブルA」という実体が各ワークスペース（A部署用、B部署用など）に完全に閉じ込められていました。\n*   **問題点:** B部署が「A部署のテーブルA」を使いたい場合、データを丸ごとコピーしてB部署用のS3（ストレージ）とワークスペースに持ってくる必要があり、二重管理（サイロ化）と莫大なコピーコストが発生していました。\n\n## 2. Unity Catalog のアーキテクチャ（3層構造）\nUnity Catalog (UC) は、すべてのワークスペース、すべてのアカウントにまたがる**単一の巨大なガバナンス・レイヤー（カタログ）**です。\n\nUCを有効にすると、データへのアクセスパスはこれまでの「テーブル名だけ」から、**「3層名前空間」**へと昇華します。これはRDB（SQL Server等）に非常に近い概念です。\n\n`SELECT * FROM [カタログ名].[スキーマ名].[テーブル名]`\n\n*   **カタログ (Catalog)**: 最上位のコンテナ。物理的な環境（例：`prod`＝本番環境、`dev`＝開発環境、`sandbox`＝実験場）で分けるのが実務のベストプラクティスです。特定のワークスペース（開発部用）からは `dev` と `sandbox` カタログだけ見え、本番ジョブからは `prod` を参照する、といった強固な分離ができます。\n*   **スキーマ (Schema)**: データベースに相当。部署や機能（例：`sales`, `marketing`, `hr`）で分けます。\n*   **テーブル (Table)**: 実際のデータ（`daily_summary` 等）。\n\n## 3. 実務での判断：「マネージド」か「外部」か？\nUCにおいてデータエンジニアが最初に設計を迫られるのが、「このテーブルの実体（クラウド上のParquetファイル）は、Databricksに完全に管理させるか、それとも自分たちのストレージ上に残すか」です。\n\n### 🛡️ マネージドテーブル (Managed Tables)\n*   **定義:** データの実体（S3ディレクトリ）も、メタデータ（列などの情報）も、全てUnity Catalogが作成した管理下ストレージに任せるテーブル。\n*   **特徴:** `DROP TABLE` を実行すると、**クラウド上の実体ファイルも完全に消去（30日以内）されます。**\n*   **実務での使い所:** Databricks内で完結しているGold層の集計テーブルや、Silver層の中間テーブル等は全てこれにします。パフォーマンスも最高にチューニングされます。\n\n### 🔗 外部テーブル (External Tables) / 外部ロケーション\n*   **定義:** データの実体はユーザー企業が運用する既存のS3バケット上にあり、UCには「そのS3のメタデータ定義とアクセス権限（External Location）」だけを登録するテーブル。\n*   **特徴:** `DROP TABLE` を実行しても、UCの登録が消えるだけで**S3上の実体ファイルはそのまま残ります。**\n*   **実務での使い所:** Bronze層の入り口（他システムからS3に直接ファイルが置かれる場所）や、Databricks以外のツール（SnowflakeやRedshift等）からも同時に同じデータを参照したい場合は必ずこれを使います。\n\n## 4. 動的アクセスコントロールによる行・列レベルのセキュリティ\nUCを使えば、単に「このテーブルを見せる・見せない」という大雑把な `GRANT SELECT` だけでなく、SQL関数を使った「ダイナミック（動的）なビュー」が作れます。\n\n> [!TIP]\n> **実務での強力なユースケース**\n> 1つの `sales_master` テーブルを全社に公開します。\n> しかし、クエリを実行したユーザー（ログインしているメールアドレス等）の所属を見て、**「人事部の人間がクエリを実行した場合だけ、給与カラムを `***` にマスキングする」「A支店の店長がクエリを実行した時は、自動的にWHERE句に `branch = A` が挿入され、他店のデータが消えるようにする」**といった高度な制御（行レベル・列レベルセキュリティ）が、たった一つのテーブル（ビュー）の定義だけで実現できます。\n"}, {"id": "Week4_02_MLflow_and_ExperimentTracking.md", "title": "02_MLflow_and_ExperimentTracking", "content": "# 第4週②：MLflowによる機械学習とメタデータ管理の自動化\n\nデータ基盤（メダリオンアーキテクチャとUnity Catalog構成）がクリーンになり、高い品質のGold層データが完成しました。次に行うべきは、そのデータを用いたAI／機械学習（ML）の実装です。\nここでは、Databricksが買収・統合した世界最強のML運用ツール「**MLflow**」の実務的な使い方を学びます。\n\n## 1. モデル開発における「属人化とカオスの歴史」\nデータサイエンティストがモデル（例：来月の売上予測モデル）を作る時、Pythonノートブック上で「パラメータAを0.01、パラメータBを100にして...」と何度も学習（実験）をぶん回します。\n*   **問題点:** 「1週間前に作った、あの『一番精度が良かった時のモデル』って、パラメータいくつで回したやつだっけ？」「そのモデルの重みファイル（pklファイル）、どこに保存した？」\n*   **結果:** エクセルに手書きでパラメータと精度をメモし、ファイル名が `sales_model_v3_final_final2.pkl` といった形になり収拾がつかなくなりました。\n\n## 2. MLflowによる「実験トラッキング（Experiment Tracking）」\nこのカオスを解決するのがMLflowのトラッキング機能です。コードに数行足すだけで、毎回の学習(Run)の「パラメータ（設定値）」「メトリクス（精度や誤差）」「アーティファクト（出来上がったモデルのファイルやグラフ画像）」を一元的に自動記録します。\n\n### 実務での最も強力な機能：オートロギング（Auto-logging）\nかつては `mlflow.log_param(\"learning_rate\", 0.01)` のように一つ一つ手書きしていましたが、現在は主要なフレームワーク（scikit-learn, XGBoost, PyTorch等）に対して、**一行書くだけで全てのパラメータと結果を自動記録**してくれます。\n\n```python\nimport mlflow\nimport mlflow.sklearn\n\n# これを1行書くだけで、以降のsklearnによる学習の裏側を全て自動記録！\nmlflow.sklearn.autolog()\n\n# 「experiment_v1」という名前の実験空間で記録を開始\nwith mlflow.start_run(run_name=\"experiment_v1\"):\n    model = RandomForestRegressor(n_estimators=100)\n    model.fit(X_train, y_train)\n    \n    # 📝 画面右上のアイコン（Experiment）をクリックすると、\n    # 決定木の深さ、使用したデータセット、MAE・RMSEなどの各種誤差メトリクスが\n    # 全て美しいUI（グラフ）にまとめられ、過去の実験との比較が一瞬で可能になります！\n```\n\n## 3. Unity Catalogと統合された「モデルレジストリ（Model Registry）」\nMLflowの「Model Registry」は、**「完成した学習済みモデルを、データテーブルと同じようにカタログ管理・共有する」**機能です。\n\n> [!IMPORTANT]\n> **実務でのアーキテクチャ（Models in Unity Catalog）**\n> 最新のDatabricksでは、機械学習モデルもUnity Catalogの一部として、テーブルと同様の「3層構造（カタログ・スキーマ・モデル）」で保存します。\n>\n> 例えば、データサイエンティストが頑張って精度99%の売上予測モデルを作り、それを `prod.sales.sales_forecasting_model` という名前でUCに登録（Register）したとします。\n> 別の部署のエンジニアは、Pythonから `mlflow.pyfunc.load_model(\"models:/prod.sales.sales_forecasting_model/1\")` と呼び出すだけで、すぐに本番システムへそのモデルをデプロイして予測を実行できます。\n>\n> テーブルと同様に「誰にこのモデルを使わせるか（GRANT EXECUTE ON MODEL...）」という権限設定ができるため、企業全体でのAIの使い回し（MLOps推進）が圧倒的に加速します。\n"}, {"id": "Week4_03_MosaicAI_and_GenAI.md", "title": "03_MosaicAI_and_GenAI", "content": "# 第4週③：Mosaic AIと「Data Intelligence Platform」への進化\n\nDatabricksは現在、「単なるLakehouse」にとどまらず、基盤全体に生成AIやLLM（大規模言語モデル）の知能を統合させた「**Data Intelligence Platform**」へと進化しています。（これは、MosaicMLという世界トップクラスの生成AI企業を買収したことで劇的に加速しました）。\n\nここでは、最新のDatabricksエコシステムにおける「3つの生成AI機能」と、実務での活用法を解説します。\n\n---\n\n## 1. AI Functions (SQL組み込みのAI関数)\nデータアナリストが100万行の顧客レビュー（テキスト）を分析したい時、これまではPythonで自然言語処理ライブラリを組み込み、重い処理を書く必要がありました。\n**AI Functions** を使うと、普通のSQLクエリの中に直接「LLM（言語モデル）に指示を出す関数」を書き込み、表データ全体に対して一括でAI処理を実行させることができます。\n\n```sql\n-- テーブル内の「review_text（顧客の生の声）」カラムをLLMに読ませて、\n-- それがポジティブかネガティブか、あるいは特定の課題かを一瞬で判定させる！\nSELECT \n    customer_id,\n    review_text,\n    ai_analyze_sentiment(review_text) AS sentiment,\n    ai_generate_text(\n        \"以下のレビューから、最も不満に思っている箇所を10文字以内で抽出して: \" || review_text\n    ) AS problem_summary\nFROM gold_customer_reviews;\n```\n> [!TIP]\n> **実務での破壊力**\n> コールセンターの通話録音の文字起こしデータなどを、パイプライン（Silver層 -> Gold層）の途中でAI Functionsを通すだけで、**「不満カテゴリ分類」や「要約」といった高度なAI処理済みのGoldテーブル**が、SQLエンジニア単独で数十分で構築できてしまいます。\n\n## 2. Mosaic AI Vector Search (ベクトル検索) と RAG\n企業内で「社内規程PDFや過去のトラブル報告書を読み込ませた、社内専用のChatGPT（RAGシステム）」を作る機運が高まっています。しかし通常、ベクトルデータベース（Pinecone等）を外部に別途契約して連携させるのは、セキュリティ審査や運用コストの壁がありました。\n\nDatabricksには **Mosaic AI Vector Search** という「Unity Catalog上に存在するフルマネージドのベクトルDB」が内蔵されています。\n*   **特徴:** Unity Catalog上のDeltaテーブルと同期し、PDFテキストなどを自動的にベクトル化（埋め込み）して一元管理します。\n*   **実務の強み:** データが自社のLakehouse基盤（VPC等）から外部へ一切漏れないため、機密情報を扱う社内RAGの構築に最適です。「Models in UC」で管理されている自社製の埋め込みモデル等を使うことも可能です。\n\n## 3. Databricks Genie（データ非専門家向けの自然言語BI）\nこれまでは、どんなに完璧なGold層を作っても、ビジネス側（営業担当者など）はBIツール（TableauやPowerBI）の決まったダッシュボードのグラフしか見ることができませんでした。「ちょっと違う切り口（例：先週の関東地方の特定商品の店舗別売上）が見たい」と思っても、データアナリストに依頼してSQLを書いてもらうまで数日待つ必要がありました。\n\n**Databricks Genie** は、この溝を埋める「チャット型のエージェント」機能です。\n*   営業担当者がチャット画面で「先週の関東地方の、商品Aの売上を店舗別に棒グラフで出して」と自然言語（日本語・英語）で打ち込みます。\n*   Genieエージェントが、Unity Catalogに登録されているメタデータ（テーブルとカラムの意味）を理解し、**自律的にSQLを生成して基盤にクエリを投げ、結果の表やグラフを数秒で返します。**\n\n> [!IMPORTANT]\n> **アーキテクトとしての総括**\n> Databricksが目指す究極の世界は、\n> 「エンジニアがAuto LoaderとDLTで**堅牢なパイプライン(SSOT)**を作り」\n> 「Unity Catalogで**誰もが使えるように安全にカタログ化**し」\n> 「ビジネスサイドがGenieやAI Functionsで**自由にデータを引き出して価値に変える**」\n> という、エコシステム全体の民主化です。ここまで設計できれば、あなたは真のDatabricksアーキテクトです！\n"}, {"id": "Week4_04_HandsOn_Master.md", "title": "04_HandsOn_Master", "content": "# 第4週④：週次ハンズオン・マスターコード (MLflowとガバナンス編)\n\n1ヶ月のDatabricks学習の最終週です。ここでは、データエンジニアリングの「その先」にある、MLflowによるモデルの実験記録（ロギング）と、Unity Catalogの概念を疑似体験するPySparkコードを実行します。\n\nDatabricks Community Editionで新規ノートブックを作成し、以下のコードを上から順に実行（Shift+Enter）してください。これを終えれば、1ヶ月プログラムは完走です！\n\n---\n\n### Step 1: 機械学習用の擬似データ（Gold層の集計結果）の準備\n実務において、「過去の広告費用（X）」から「来月の売上（Y）」を予測するモデルを作るための綺麗なデータセットが、すでにメダリオンアーキテクチャのGold層に存在していると仮定します。\n\n```python\nfrom pyspark.ml.linalg import Vectors\nfrom pyspark.ml.regression import LinearRegression\nimport mlflow\nimport mlflow.spark\n\n# ダミーの「広告費 vs 売上」データを作成\n# (広告費(特徴量), 売上(ラベル)) の形式でベクトル化してSpark DataFrameにします\ndata = [\n    (100.0, Vectors.dense([10.0])),  # 広告費10なら売上100\n    (150.0, Vectors.dense([15.0])),  # 広告費15なら売上150\n    (200.0, Vectors.dense([22.0])),  # 広告費22なら売上200\n    (300.0, Vectors.dense([28.0])),\n    (400.0, Vectors.dense([45.0]))\n]\n\ngold_df = spark.createDataFrame(data, [\"label\", \"features\"])\nprint(\"✅ 学習用データ (Gold層) の準備完了！\")\ndisplay(gold_df)\n```\n\n### Step 2: MLflow トラッキングの魔法（自動ロギング）を体験\nMLflowを使って、「単なるPythonでの機械学習モデルの訓練」から「エンタープライズレベルでのモデルとパラメータの【記録】業務」へと昇華させます。\n\n```python\n# 👉 PySpark MLlib用のオートロギング機能（これ一行でパラメータ・精度・モデル実体が全て記録される）\nmlflow.pyspark.ml.autolog()\n\nprint(\"🚀 MLflow実験スタート...\")\n\n# 「my_first_experiment」という名前の空間に、この学習の全てを封じ込める\nwith mlflow.start_run(run_name=\"my_first_experiment\"):\n    \n    # 1. アルゴリズム（線形回帰）の定義。ハイパーパラメータを設定（例: 正規化パラメータ = 0.1）\n    lr = LinearRegression(maxIter=10, regParam=0.1, elasticNetParam=0.5)\n    \n    # 2. モデルの学習（Fit） ※通常はここで数時間かかったりする\n    model = lr.fit(gold_df)\n    \n    print(\"🎯 学習完了！ モデルの重み・パラメータ・RMSEなどの誤差精度がMLflowへ自動送信されました。\")\n```\n\n> [!TIP]\n> **🚀 体感ポイント**\n> 上のセルの実行が終わったら、**ノートブック画面の右上にある「フラスコアイコン（Experiment / 実験）」** をクリックして引き出してみてください！\n> そこに `my_first_experiment` という行があり、クリックするとMLflowの美しいUI管理画面へ遷移します。\n> スクリプト内には一切「この数値を記録して」と書いていないのに、`regParam=0.1` という設定値や、モデルそのもの（アーティファクト）が安全に保管されているのが確認できるはずです！\n\n---\n\n### Step 3: Unity Catalog (UC) の3層名前空間・権限付与の概念（コードリーディング）\n※ *注意: Community EditionではUnity Catalogの中核機能は無効化されているため、以下のSQLはエラーになる可能性がありますが、「実務現場でアーキテクトが何を書いているか」を理解するための読み物として実行・確認してください。*\n\n```sql\n%sql\n-- 1. カタログ (最上位階層) を作成（例：本番環境）\n-- CREATE CATALOG IF NOT EXISTS prod_catalog;\n\n-- 2. その中にスキーマを作成（例：マーケティング部）\n-- CREATE SCHEMA IF NOT EXISTS prod_catalog.marketing;\n\n-- 3. （仮想）作成したテーブルに対して、権限を与える（GRANT）\n-- 以下のコマンドで、\"data_analyst_group\" に所属する全社員に、該当テーブルの「SELECT（読み取り）」権限だけを与えます\n-- GRANT SELECT ON TABLE prod_catalog.marketing.gold_sales_summary TO `data_analyst_group`;\n\n-- 4. 逆に、データサイエンティストには「モデルの作成と管理」権限を与える\n-- GRANT CREATE MODEL ON SCHEMA prod_catalog.marketing TO `data_scientist_group`;\n```\n\n---\n🎉 **祝！Databricks 1ヶ月プログラム完走！** 🎉\n\nあなたは今、単に「PySparkが書ける人」ではなく、\n「**なぜDelta Lakeでトランザクションが担保されるのか**」\n「**なぜZ-Orderや_delta_logの仕組みがパフォーマンスを向上させるのか**」\n「**なぜメダリオン構造とUCによるガバナンスが必要なのか**」\nというアーキテクチャの根幹を語れる、**実務レベルのデータエンジニア／アーキテクト**へと成長しました！\n\n実務で壁にぶつかったら、いつでもまたこの資料（Antigravity・Gemini）に戻ってきて壁打ちをしてください。お疲れ様でした！\n"}]};